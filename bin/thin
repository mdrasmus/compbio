#!/usr/bin/env python

import sys

if len(sys.argv) < 1:
    print >>sys.stderr, "thin <density>"
    sys.exit(1)

density = int(sys.argv[1])

i = 0
for line in sys.stdin:
    if i == 0:
        sys.stdout.write(line)
    i = (i + 1) % density

   
