#!/usr/bin/env python

import os
import sys

#cd ~/projects/rasmus && cvs update -d

#chmod +x ~/projects/rasmus/bin/*
#chmod +x ~/projects/compbio/bin/*


os.system("cd ~/projects/rasmus && git pull")


if "spidir" in sys.argv:
    os.system("cd ~/projects/rasmus/spidir && make clean && make install DEBUG=1")

